<% if (title) { %>
<h1><%= title %></h1>
<div class="managment-links">
  <a href="/inv/addclassification">Add New Classification</a>
  <a href="/inv/addinventory">Add New Inventory View</a>
</div>
<h2>Manage Inventory</h2>
<p>Choose a classification to see those inventory items.</p>
<%-classificationSelect%>
<table id="inventoryDisplay"></table>
<noscript>JavaScript must be enabled to use this page.</noscript>

<% } else { res.redirect('/'); } %> <% if (errors) { %>
<ul class="notice">
  <% errors.array().forEach(error => { %>
  <li><%= error.msg %></li>
  <% }) %>
</ul>
<% } %>
<script src="../../js/inventory.js"></script>
